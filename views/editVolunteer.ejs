<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link href="https://getbootstrap.com/docs/5.3/assets/css/docs.css" rel="stylesheet">
    <title>Edit Volunteer</title>
</head>

<body>

    <div class="w-100 p-5 pb-0 d-flex">
        <h1>
            Edit Volunteer
        </h1>
    </div>

    <div class="w-100 px-5 d-flex">
        <form action="/updateVolunteer/<%= singleVolunteer.id %>" method="post" class="mt-5">
            <div class="row">
                <input type="text" name="id" value="<%= singleVolunteer.id %>" hidden />

                <div class="col px-1">
                    <input type="text" class="form-control mt-2" name="fname"
                        value="<%= singleVolunteer.volunteer.split(' ')[0] %>" placeholder="First Name"
                        aria-label="First Name">
                    <input type="email" class="form-control mt-2" name="email" value="<%= singleVolunteer.email %>"
                        placeholder="Email Address" aria-label="Email">
                    <textarea class="form-control mt-2" name="skills" placeholder="Skills"
                        aria-label="Skills"><%= singleVolunteer.skills %></textarea>
                </div>
                <div class="col px-1">
                    <input type="text" class="form-control mt-2" name="lname"
                        value="<%= singleVolunteer.volunteer.split(' ')[1] %>" placeholder="Last Name"
                        aria-label="Last Name">
                    <input type="number" class="form-control mt-2" name="contact" value="<%= singleVolunteer.contact %>"
                        placeholder="Contact Number" aria-label="Contact">
                </div>
                <div class="col-12 mt-3 px-1">
                    <button type="submit" class="btn btn-success">Update</button>
                </div>
            </div>
        </form>
    </div>
</body>

</html>